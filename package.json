{
  "name": "ci-dash",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "babel-core": "^6.26.0",
    "express": "^4.16.3",
    "nock": "^9.2.3",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1",
    "superagent": "^3.8.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "server": "babel-node src/api/index.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\""
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "concurrently": "^3.5.0"
  },
  
  "proxy": "http://localhost:8080"
}
